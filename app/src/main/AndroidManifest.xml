<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mazeit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MazeIt">
        <activity
            android:name=".RecList_activity"
            android:exported="true" />
        <activity
            android:name=".show_records"
            android:exported="true" />
        <activity
            android:name=".testthing"
            android:exported="true" />
        <activity
            android:name=".levelmenu"
            android:exported="true" />
        <activity
            android:name=".MazeGame"
            android:exported="true" />
        <activity
            android:name=".MazeTest"
            android:exported="true"
            android:label="@string/title_activity_maze_test"
            android:theme="@style/Theme.MazeIt.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>